def solution(players, callings):
    answer = players.copy()
    for call in callings:
        i = answer.index(call)
        i -= 1
        
        unit = answer[i+1]  # i+1 인덱스의 값
        answer[i+1] = answer[i]
        answer[i] = unit
        
    return answer
